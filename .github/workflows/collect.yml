name: Hourly adaptive intelligence collection

# The hourly trigger is only a heartbeat. backend/scheduler.py decides which
# persisted per-(source, domain) collectors are actually due on each run.
on:
  schedule:
    - cron: '0 * * * *'   # hourly, UTC
  workflow_dispatch:
    inputs:
      hn_only:
        description: 'Use HN only (no Reddit credentials required)'
        type: boolean
        default: false

# Least-privilege: this workflow only needs to read the repo, manage its
# own cache, and read/list workflow artifacts (to restore the database
# backup). No write access to repository contents is needed. See the V1
# security review's CI/CD permissions finding.
permissions:
  contents: read
  actions: read

# The CI snapshot is one shared persistence authority. Do not cancel an
# in-progress collector: it may already have committed useful scheduler state.
concurrency:
  group: bia-collection-canonical-snapshot
  cancel-in-progress: false

jobs:
  collect:
    name: Schedule · Collect · Extract · Detect · Report
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── Restore the database from a previous run ────────────────────────
      # The database accumulates signals across runs. Without this, every run
      # would start from scratch and lose all historical signals.
      # Uses GitHub's cache with a prefix restore key so any previous run's
      # cache is used when no exact match exists (i.e., always on the first
      # restore after an update).
      - name: Restore database cache
        uses: actions/cache@v4
        with:
          path: backend/data/bia.db
          key: bia-database-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            bia-database-${{ runner.os }}-

      # ── Restore the canonical durable snapshot ──────────────────────────
      # Cache is an optimization only. Canonical artifacts use a distinct name
      # so format is known from metadata without downloading retained history.
      # A canonical artifact is authoritative, including one from a run that
      # failed after committing scheduler/collector state. Before the first
      # canonical artifact exists, only the newest legacy artifact is fetched
      # for one safe migration. A retrieval failure for an authority candidate
      # stops this run; only a genuine no-artifact first-run may use cache.
      - name: Restore canonical database artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -o pipefail
          ARTIFACT_DIR="$(mktemp -d)"
          CANONICAL_ARTIFACT_NAME="bia-database-canonical"
          LEGACY_ARTIFACT_NAME="bia-database-backup"
          ARTIFACTS_ENDPOINT="repos/${GITHUB_REPOSITORY}/actions/artifacts?per_page=100"

          # Pagination reads metadata only; sort it explicitly before choosing
          # one artifact so no artifact-content scan is needed for discovery.
          CANONICAL_ARTIFACT_RECORD="$(gh api --paginate --slurp "$ARTIFACTS_ENDPOINT" | jq -r '[.[].artifacts[] | select(.name == "bia-database-canonical" and .expired == false)] | sort_by(.created_at) | reverse | .[0] | select(.) | [.id, .workflow_run.id] | @tsv')" || {
            echo "Unable to list canonical artifacts; refusing cache fallback." >&2
            exit 1
          }

          if [ -n "$CANONICAL_ARTIFACT_RECORD" ]; then
            IFS=$'\t' read -r CANONICAL_ARTIFACT_ID CANONICAL_RUN_ID <<< "$CANONICAL_ARTIFACT_RECORD"
            echo "Restoring selected canonical artifact $CANONICAL_ARTIFACT_ID from run $CANONICAL_RUN_ID"
            if ! gh run download "$CANONICAL_RUN_ID" -n "$CANONICAL_ARTIFACT_NAME" -D "$ARTIFACT_DIR"; then
              echo "Canonical artifact retrieval failed; refusing cache fallback." >&2
              exit 1
            fi
            CANONICAL_SNAPSHOT="$(find "$ARTIFACT_DIR" -type f -name bia-latest.db -print -quit)"
            if [ -z "$CANONICAL_SNAPSHOT" ]; then
              echo "Selected canonical artifact did not contain bia-latest.db; refusing cache fallback." >&2
              exit 1
            fi
            mkdir -p backend/data/backups
            cp "$CANONICAL_SNAPSHOT" backend/data/backups/bia-latest.db
            echo "BIA_SNAPSHOT_FORMAT=canonical" >> "$GITHUB_ENV"
          else
            LEGACY_ARTIFACT_RECORD="$(gh api --paginate --slurp "$ARTIFACTS_ENDPOINT" | jq -r '[.[].artifacts[] | select(.name == "bia-database-backup" and .expired == false)] | sort_by(.created_at) | reverse | .[0] | select(.) | [.id, .workflow_run.id] | @tsv')" || {
              echo "Unable to list legacy artifacts; refusing cache fallback." >&2
              exit 1
            }
            if [ -z "$LEGACY_ARTIFACT_RECORD" ]; then
              echo "No prior database artifact found -- first run; cache/fresh start is allowed"
              exit 0
            fi

            IFS=$'\t' read -r LEGACY_ARTIFACT_ID LEGACY_RUN_ID <<< "$LEGACY_ARTIFACT_RECORD"
            echo "Downloading selected legacy artifact $LEGACY_ARTIFACT_ID from run $LEGACY_RUN_ID for migration"
            if ! gh run download "$LEGACY_RUN_ID" -n "$LEGACY_ARTIFACT_NAME" -D "$ARTIFACT_DIR"; then
              echo "Legacy artifact retrieval failed; refusing cache fallback." >&2
              exit 1
            fi
            echo "BIA_SNAPSHOT_FORMAT=legacy" >> "$GITHUB_ENV"
            echo "BIA_LEGACY_SNAPSHOT_DIR=$ARTIFACT_DIR" >> "$GITHUB_ENV"
            echo "No canonical artifact found; migrating the newest valid snapshot from the selected legacy artifact"
          fi

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r backend/requirements.txt

      - name: Migrate selected legacy database snapshot
        if: ${{ env.BIA_SNAPSHOT_FORMAT == 'legacy' }}
        run: python -c "import os, sys; from pathlib import Path; sys.path.insert(0, 'backend'); import persistence; persistence.migrate_legacy_snapshot(Path(os.environ['BIA_LEGACY_SNAPSHOT_DIR']))"

      - name: Install validated canonical database snapshot
        if: ${{ env.BIA_SNAPSHOT_FORMAT != '' }}
        run: python -c "import sys; sys.path.insert(0, 'backend'); import persistence; persistence.restore_canonical_snapshot()"

      # A failure before this point must not republish a cache copy as a new
      # canonical artifact. Once this point is reached, collect.py owns
      # snapshotting transactionally committed database state in its finally.
      - name: Mark persistence authority ready
        id: persistence_authority
        run: echo "ready=true" >> "$GITHUB_OUTPUT"

      # ── Run the intelligence pipeline ───────────────────────────────────
      - name: Run collection pipeline
        run: |
          # Determine flags
          FLAGS=""

          # Manual trigger: use workflow input for hn_only
          if [ "${{ github.event.inputs.hn_only }}" = "true" ]; then
            FLAGS="$FLAGS --hn-only"
          fi

          python backend/collect.py $FLAGS
        env:
          REDDIT_CLIENT_ID:     ${{ secrets.REDDIT_CLIENT_ID }}
          REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
          SEC_EDGAR_USER_AGENT: ${{ secrets.SEC_EDGAR_USER_AGENT }}
          GITHUB_TOKEN:         ${{ github.token }}
          BIA_DATA_DIR:         backend/data

      # ── Durable canonical snapshot ───────────────────────────────────────
      # One SQLite-backup-API snapshot is uploaded, never the historical
      # backup directory. GitHub artifacts provide run-level retention; the
      # latest completed artifact is the cold-start durability fallback. A
      # failed run may still contain valid committed scheduler/collector state;
      # this snapshot is evidence state, not a success verdict for the run.
      - name: Upload canonical database artifact
        if: ${{ always() && steps.persistence_authority.outputs.ready == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: bia-database-canonical
          path: backend/data/backups/bia-latest.db
          retention-days: 90
          if-no-files-found: warn

      # ── Upload pipeline summary ─────────────────────────────────────────
      - name: Write job summary
        if: always()
        run: |
          echo "## BIA-OS Adaptive Scheduler Run" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Run:** \`${{ github.run_id }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered by:** \`${{ github.event_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Date:** $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_STEP_SUMMARY
          if [ -f backend/data/bia.db ]; then
            DB_SIZE=$(du -sh backend/data/bia.db | cut -f1)
            echo "- **Database size:** $DB_SIZE" >> $GITHUB_STEP_SUMMARY
          fi
