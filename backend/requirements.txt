# BIA-OS backend dependencies
# Pin major versions to avoid surprise breakage.
# Run: pip install -r requirements.txt

# ── API server ─────────────────────────────────────────────────────────────
fastapi==0.115.0          # Web framework; async-native, fast, excellent docs
uvicorn[standard]==0.30.6 # ASGI server with websocket and reload support
pydantic==2.8.2           # Data validation (used at the API boundary only)

# ── HTTP client ────────────────────────────────────────────────────────────
# requests is synchronous — fine for collectors which run in background tasks.
# Do not use requests inside async route handlers.
requests==2.32.3

# ── Data sources ───────────────────────────────────────────────────────────
praw==7.7.1               # Official Reddit API wrapper (read-only, free)
# HN uses the Firebase public API via requests — no extra package needed.

# ── Testing ────────────────────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.23.8    # Async test support for FastAPI route tests
httpx==0.27.2             # Async HTTP client used in FastAPI test client

# ── Development utilities ──────────────────────────────────────────────────
# Nothing optional. Keep the dependency count minimal.
